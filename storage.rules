rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow all authenticated users to upload and download
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
